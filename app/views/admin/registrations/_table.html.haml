.card
  %table.table.table-responsive-md
    %thead.thead-light
      %tr
        %th ID
        %th Status
        %th Race
        %th Name
        %th Email
        %th

    %tbody
      - if @registrations.any?
        - @registrations.each do |registration|
          = render "registration_row", registration: registration.decorate
      - else
        %tr
          %td{colspan: 6} No registrations match this filter criteria.

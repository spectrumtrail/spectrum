%h3.heading.h3.text-white.pt-3
  Welcome back!
%h6.text-white.pb-5
  You can sign in to your account here.
= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: { class:"form-tertiary pr-2" }) do |f|
  .form-inputs
    = f.input :email,
      required: false,
      autofocus: true,
      input_html: { autocomplete: "email" }
    = f.input :password,
      required: false,
      input_html: { autocomplete: "current-password" }
    = f.input :remember_me, as: :boolean if devise_mapping.rememberable?
  .form-actions
    = f.button :submit, "Sign In", class: "btn btn-block"

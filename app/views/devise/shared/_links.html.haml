-# = link_to "Go back", :back, class: "d-block text-secondary"
-# - if controller_name != 'sessions'
-#   = link_to "Sign In", new_session_path(resource_name), class: "d-block text-white"
-# - if devise_mapping.registerable? && controller_name != 'registrations'
-#   = link_to "Sign Up", new_registration_path(resource_name), class: "d-block text-white"
-# - if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
-#   = link_to "Forgot your password?", new_password_path(resource_name), class: "d-block text-white"
-# - if devise_mapping.confirmable? && controller_name != 'confirmations'
-#   = link_to "Missing account confirmation instructions?", new_confirmation_path(resource_name), class: "d-block text-white"
-# - if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
-#   = link_to "Missing unlock instructions?", new_unlock_path(resource_name), class: "d-block text-white"
